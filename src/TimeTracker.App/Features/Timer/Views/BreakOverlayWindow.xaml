<Window
    x:Class="TimeTracker.App.Features.Timer.Views.BreakOverlayWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Time Tracker Break">
    <Border x:Name="OverlayRoot" Padding="16" Background="#334155">
        <Grid ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="16">
                <TextBlock x:Name="MessageText" x:Uid="WorkingMessage" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock x:Name="RemainingText" FontSize="20" VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button x:Name="SkipButton" x:Uid="SkipButton" Click="SkipButton_OnClick" />
                <Button x:Name="RestButton" x:Uid="RestButton" Click="RestButton_OnClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
