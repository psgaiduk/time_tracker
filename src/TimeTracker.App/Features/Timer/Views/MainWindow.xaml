<Window
    x:Class="TimeTracker.App.Features.Timer.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Time Tracker">
    <Grid Padding="32" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Uid="AppTitle" FontSize="28" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Text="{x:Bind ViewModel.RemainingText, Mode=OneWay}" FontSize="64" HorizontalAlignment="Center" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="12">
            <Button x:Uid="StartButton" Command="{x:Bind ViewModel.StartCommand}" />
            <Button x:Uid="SkipButton" Command="{x:Bind ViewModel.SkipCommand}" />
            <Button x:Uid="RestButton" Command="{x:Bind ViewModel.RestCommand}" />
        </StackPanel>
    </Grid>
</Window>
